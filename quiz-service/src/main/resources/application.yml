
server:
  port: 8089

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/quizs
    username: postgres
    password: 1729
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  application:
    name: quiz-service
